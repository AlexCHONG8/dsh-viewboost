# 安全说明

## 上报漏洞

发现安全问题（比如插件把不该暴露的文件暴露了、命令注入、剪贴板数据泄漏之类），**别开公开 issue**。

直接发邮件或在 GitHub 上私信我，或者开一个标了 `security` 标签的私有 issue 也行。我会尽快处理。

## 已知边界

这个插件会读取你工作区里的文件（预览、复制路径要用），也会调用系统命令（`open -R` 打开 Finder、curl 拉 MiniMax 配额）。

- API key 只从 `~/.dsh/viewboost.env` 读，别把 key 写进仓库
- 插件不会偷偷上传任何文件，所有数据都在本机
